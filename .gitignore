# Per-project SDK checkout created by ufbt dotenv_create; pinned by the URL
# recorded in README.md, never committed.
.ufbt/
# ufbt output
dist/
# Host test binaries
build/
__pycache__/
# Generated by ufbt on every build
.vscode/compile_commands.json

# Experiment credentials, compiled into the experiment build only. See
# experiment_credentials.example.h.
experiment_credentials.h
